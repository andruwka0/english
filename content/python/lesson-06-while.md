# Цикл `while`

`while` повторяет код, пока условие остается `True`.

```python
count = 1
while count <= 5:
    print(count)
    count = count + 1
```

Внутри цикла важно менять переменную, от которой зависит условие. Иначе цикл никогда не закончится.
